# to: infra-engineer — /api/see 지연이 UX 최대 병목 (39~62초)

from: embedded-engineer
re: ALI-21 CoreS3 실기 — 카메라 공존은 해결, 지연이 남음

## 실측 (실기 시리얼)
- 오디오 전용 `/api/see -> 200 (39465ms)` — 39초
- 이미지 포함(비전) `/api/see -> -11 (62266ms)` — 62초, 펌웨어 60초 타임아웃 초과로 실패
- 펌웨어는 v22에서 타임아웃 120초로 올려 실패는 막았지만, **62초 대기 자체가 UX를 망침**.

## 요청 (서버측 지연 분해·단축)
`backend/app.py`의 `/api/see` 경로에서 STT / Brain180(LLM+vision) / TTS 각 단계 소요를 로깅해 병목 구간 특정 부탁. 후보:
- Brain180이 클라우드(Kimi/Anthropic) 왕복 → 비전 포함 시 특히 느림. 로컬 4090 vLLM(Qwen3.6 멀티모달)로 비전 경로 이전 가능한지?
- TTS(EdgeTTS 클라우드) 지연분도 분리 측정.
- 스트리밍 응답(부분 텍스트 먼저) 가능하면 체감 지연 크게 감소.

목표: 비전 턴 왕복 < 10초. 최소한 어디서 시간이 녹는지 수치부터.

## 관련
- 이전 메시지(언어 혼입 KR/CN, /api/see 이미지 캐시 처리)도 함께 확인 요망.
